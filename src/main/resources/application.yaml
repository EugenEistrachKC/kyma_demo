spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: kyma4ever
  jpa:
    show-sql: true
    hibernate:
      dialect: ${org.hibernate.dialect.H2Dialect:DB_DIALECT}
      ddl-auto: update
